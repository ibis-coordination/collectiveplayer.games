#!/bin/bash -e

# Prepare the database whenever the container is booting the Rails app,
# whether invoked as `./bin/rails server` directly or via Thruster
# (`./bin/thrust ./bin/rails server`, our production CMD).
if [[ "$*" == *"bin/rails server"* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
