development:
  adapter: async

test:
  adapter: test

# Uses solid_cable's Postgres-backed adapter in production (LISTEN/NOTIFY
# via the primary DB; no separate Redis or cable DB needed).
production:
  adapter: solid_cable
  polling_interval: 0.1.seconds
  message_retention: 1.day
